div id="post-id-#{post.id}"
  .col
    .card
      = image_tag post.post_images.first.image_url, class: 'card-img-top', size: '300x300'
      .card-body
        ul class='list-inline'
          li class='list-inline-item'
            = post.user.name
          li class='list-inline-item'
            = l post.created_at, format: :short